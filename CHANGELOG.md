# Changelog

このプロジェクトの主な変更を、新しい順に記録します。
過去の履歴は Git のコミットと `pyproject.toml` のバージョン変更から整理しています。
日付はリリース公開日ではなく、該当変更のコミット日です。
同じバージョンのまま追加された変更も、そのバージョンの項目に含めます。

## Unreleased — 0.3.6

### 削除

- `generation` 配下を使わない旧設定の互換読み込みと、README の設定移行手順を削除。

- 既存ノートの節順を変更する `reorder-notes` コマンドを削除。
  新規要約の Conclusion を先に置く構成と、旧形式の検証対応は維持。

### ドキュメント

- `CHANGELOG.md` を追加。

## 0.3.5 — 2026-09-26

### 変更

- 日本語・英語の要約テンプレートを、Summary → Conclusion → Key points →
  Structuring (from abstract to concrete) → Technical terms の順に変更。
- 節の抽出と検証を更新し、旧テンプレートの来歴を検証しながら新しい節順にも対応。
- README を用途、クイックスタート、保存データ、コマンド・設定の説明に沿って再整理。

### 追加

- AI による再生成を行わず、バックアップ付きで既存要約の節を並べ替える
  `reorder-notes` コマンドを追加（0.3.6 で削除予定）。

## 0.3.4 — 2026-09-25

### 修正

- 原語字幕と翻訳字幕の判別・選択を改善し、自動字幕の言語バリエーションに対応。

## 0.3.3 — 2026-09-25

### 変更

- 字幕取得を yt-dlp のダウンロード処理に統一し、メタデータ取得と同じセッションの
  Cookie、HTTP ヘッダー、ブラウザー偽装設定を引き継ぐように変更。
- 依存関係を `yt-dlp[curl-cffi]>=2026.8.19` に更新。

### 修正

- 取得した字幕を JSON3 として検証してから保存。
- 取得失敗時に処理段階と取得済みメタデータを保持し、HTTP 429 の案内を改善。

## 0.3.2 — 2026-09-25

### 修正

- サムネイル URL を動画 ID から組み立てる標準形式に統一。

## 0.3.1 — 2026-09-25

### 変更

- 生成AIの呼び出しを `tkn-genai-bridge` に統合し、プロバイダーと生成設定を拡張。
- README を日本語の `README.md` に集約。

### 追加

- インストール済みのバージョンを表示する `--version` オプションを追加。

## 0.2.0 — 2026-09-25

### 追加

- 書き込み系コマンドに、ファイル保存・データ取得・AI 呼び出しを行わない
  `--dry-run` を追加。
- ノートの妥当性と要約プロファイルの新旧を確認する `status` コマンドを追加。
- バックアップ付きで既存ノートを移行する `migrate-notes` と、確認済み計画を適用する
  `--apply-plan` を追加。
- Codex 実行のタイムアウト設定と、タイムアウト時の診断情報を追加。

### 変更

- 要約テンプレートと出力スキーマの契約を管理し、履歴に基づく検証に対応。
- 要約の `description` を生成AIの出力項目から外し、Conclusion から設定する方式に変更。
- `build-source` と `build-summary` で `--force` を利用可能に変更。

## 0.1.0 — 2026-07-25〜2026-08-12

### 追加

- YouTube の字幕・動画情報の取得、raw の保存、文字起こしノートと要約ノートの生成。
- 保存済みデータの取り込み、ノートの検証、実行レポート、設定の初期化・表示。
- カスタム要約プロンプト、日本語・英語の要約プロファイル、テンプレートと出力スキーマ。
- 取得済み字幕と対応ノートを確認する `list` コマンド。
- 生成AIの失敗時の診断ログと、読みやすい JSON 出力・進捗表示。

### 変更

- CLI 名を `youtube-notes` から `tkn-youtube-note` に変更。
- 要約の Frontmatter、ファイル名、構造化・結論の生成指示を改善。
- 設定・データ・キャッシュの保存先と、インストール手順を整理。

### 修正

- 動画 URL のプレイリスト情報など、動画識別に不要なパラメーターを正規化時に除外。

### 削除

- GitHub Actions の CI ワークフローを削除。
