# Copy this file to ~/.tkn/youtube_note_pipeline/config.yaml for user-global settings,
# or to .tkn/config.yaml for a repository-local override.
# These paths match the built-in user-level data and state layout.

raw_root: ~/.tkn/youtube_note_pipeline/data/raw
source_root: ~/.tkn/youtube_note_pipeline/data/source
summary_root: ~/.tkn/youtube_note_pipeline/data/summary
reports_root: ~/.tkn/youtube_note_pipeline/state/reports

provider: codex
model: null
fallback_languages:
  - en
codex_executable: codex
